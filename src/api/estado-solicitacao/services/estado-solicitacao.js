'use strict';

/**
 * estado-solicitacao service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::estado-solicitacao.estado-solicitacao');
